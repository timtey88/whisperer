export default function TranscribeOptions() {
	// This component previously contained "More Options" with reset functionality
	// That functionality has been consolidated into Settings → Model Options
	// Component kept for future transcription-specific options if needed
	return null
}
